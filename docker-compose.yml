version: '3.9'

services:
  myapp:
    image: shaunak30/myapp
    container_name: myapp-container
    ports:
      - "8000:8000"
    environment:
      MONGO_URI: ${MONGO_URI}   # Injected from Jenkins
    restart: unless-stopped
